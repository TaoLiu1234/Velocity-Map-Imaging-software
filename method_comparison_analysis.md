# V1/V2/V3 方法设计分析

## 为什么 V1 检测率最高？

### V1 的设计特点

1. **宽松的峰检测阈值**
```python
# V1: _phase1_find_peaks_in_projection
height_threshold = max_val * 0.03   # 只需要3%的最大值
prominence_threshold = max_val * 0.02  # 只需要2%的prominence
```

2. **低SNR阈值**
```python
# V1: _phase1_radial_analysis
snr_threshold = 1.5 if snr_estimate < 30 else 2.5  # 低SNR时只需要1.5
```

3. **简单的预处理**
- V1 不做 Wiener 反卷积
- 只做频域背景估计和贝叶斯去噪
- 保留更多原始信号（包括弱峰）

4. **多方法融合的 sigma 估计**
```python
# V1 使用4种方法估计sigma并加权平均：
# 1. scipy peak_widths (权重3.0)
# 2. 高斯拟合 (权重2.0)
# 3. FWHM方法 (权重1.5)
# 4. 二阶矩方法 (权重1.0)
```

### V1 高检测率的原因

1. **不做激进的预处理** → 弱峰不会被滤掉
2. **低阈值** → 更容易检测到弱峰
3. **在投影面检测** → SNR更高（角向积分增强信号）
4. **宽松的形状检查** → 不会因为峰形不完美而拒绝

---

## 为什么 V2 在宽峰/低SNR时容易失败？

### V2 的设计特点

1. **激进的 Wiener 反卷积**
```python
# V2: _phase0_preprocess
sigma_cart = np.sqrt(self.sigma_psf**2 + self.sigma_pixel**2)
image_deconv = self._deconvolve_2d_wiener(image_2d, sigma_cart, nsr=nsr)
```

2. **更严格的峰检测**
```python
# V2: _phase1_find_peaks
if snr > 100:
    height_thresh = max_val * 0.03
elif snr > 30:
    height_thresh = max_val * 0.04
elif snr > 10:
    height_thresh = max_val * 0.05
else:
    height_thresh = max_val * 0.10  # 低SNR时阈值更高！
```

3. **多轮过滤**
```python
# V2 有三轮过滤：
# 1. 基本质量检查（SNR阈值）
# 2. 相对强度过滤（>3%最大高度）
# 3. 合并太近的峰（<10像素）
```

### V2 失败的原因

1. **Wiener 反卷积的问题**
   - 宽峰在频域的高频成分少
   - 反卷积会放大噪声
   - 宽峰可能被"反卷积掉"

2. **低SNR时阈值反而更高**
   - 设计意图：避免假阳性
   - 副作用：漏检弱峰

3. **多轮过滤累积效应**
   - 每轮都可能过滤掉边缘峰
   - 弱峰更容易被过滤

---

## 为什么 V3 对偏斜BR检测率高？

### V3 的设计特点

1. **Phase 0 只估算背景，不减法**
```python
# V3: DataCleaner.clean()
# V3.5 改进：subtract_background=False 时，只估算背景
# 背景 B 作为参数传递给 Phase 4，模型变为 Model + B
```

2. **物理正确的前向模型**
```python
# V3 使用完整的前向模型：
# 3D高斯 → Abel投影 → PSF卷积 → 像素化 → xy→rθ
```

3. **Poisson MLE 拟合**
```python
# V3 使用泊松最大似然估计
# 对低计数（弱峰）更敏感
```

4. **椭圆校正**
```python
# V3 检测并校正图像椭圆度
# 提高对称性，改善峰检测
```

### V3 对偏斜BR检测率高的原因

1. **Poisson MLE 对弱信号敏感**
   - 泊松分布在低计数时更准确
   - 不会像高斯假设那样低估弱峰

2. **前向模型拟合**
   - 不依赖峰检测阈值
   - 通过优化找到最佳参数
   - 即使弱峰也会被模型"看到"

3. **不做减法预处理**
   - 保留原始计数信息
   - 弱峰不会被背景减法"吃掉"

4. **全局优化**
   - 同时拟合所有峰
   - 强峰和弱峰一起优化
   - 弱峰可以"借助"强峰的信息

---

## 三种方法的设计哲学对比

| 方面 | V1 | V2 | V3 |
|------|-----|-----|-----|
| **预处理** | 轻度（频域滤波） | 激进（Wiener反卷积） | 保守（只估算背景） |
| **峰检测** | 宽松阈值 | 严格阈值+多轮过滤 | 前向模型拟合 |
| **噪声模型** | 简单（高斯） | 中等（自适应） | 物理正确（泊松） |
| **优化方法** | 局部优化 | 局部优化 | 全局优化 |
| **设计目标** | 高检测率 | 高精度 | 物理正确性 |

---

## 为什么会有这些差异？

### V1 的设计思路
- **目标**：不漏检任何峰
- **策略**：宽松阈值 + 简单预处理
- **代价**：可能有假阳性，BR精度较低

### V2 的设计思路
- **目标**：高精度重建
- **策略**：物理正确的反卷积 + 严格过滤
- **代价**：可能漏检弱峰/宽峰

### V3 的设计思路
- **目标**：物理正确的统计推断
- **策略**：前向模型 + Poisson MLE
- **代价**：计算复杂，β估计不稳定

---

## 实际应用建议

1. **如果担心漏检**：用 V1
   - 检测率最高
   - 适合探索性分析

2. **如果需要精确BR**：用 V2
   - BR精度最好
   - 适合分离良好的峰

3. **如果有偏斜BR**：用 V3
   - 对弱峰敏感
   - 适合不均匀强度分布

4. **如果峰很接近**：都困难
   - 考虑提高计数
   - 或使用更高分辨率的探测器
